name: kgtolbs_converter_offline
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.1+2

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  
  # Added dependencies
  shared_preferences: ^2.5.4
  fl_chart: ^0.69.0
  google_mobile_ads: ^7.0.0
  screenshot: ^3.0.0
  share_plus: ^13.1.0
  path_provider: ^2.1.4
  flutter_animate: ^4.5.0
  confetti: ^0.7.0
  url_launcher: ^6.3.0
  package_info_plus: ^10.1.0
  in_app_review: ^2.0.12

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  generate: true